// the type of configurations should not be sensitive like encryption key or password and other credentials 


export const config = {
    default: {
        // we are gonna use this for the login in case they don't provide a login name
        namespace:"Application"
    }
}